'use client';

import React from 'react';
import UploadForm from '~/components/UploadForm'; // adjust the path if needed

const UploadPage = () => {
  return <UploadForm mode="upload" />;
};

export default UploadPage;
